# Decision records

One file per decision that would be expensive to reverse. Each records what was
decided, what problem it solves, what was rejected and why, and what it costs.

A record is written when the decision is made and amended only by a later
record that supersedes it. Records are not edited to match the implementation:
if the implementation diverged, that is itself a decision and gets its own
record.
